version: 0.2

phases:
  install:
    commands:
      - echo "Installing Lambda build dependencies"
  build:
    commands:
      - echo "Packaging Lambda functions (placeholder)"
  post_build:
    commands:
      - echo "Lambda build complete (placeholder)"

artifacts:
  files:
    - '**/*'
